#include <stdio.h>

int main() {
     // Menyediakan buffer untuk nama pengguna

    // Meminta nama pengguna
    char nama[] = "Muhammad Putra Azky Alfani";

    // Menampilkan selamat pagi
    printf("Selamat Pagi, %s\n", nama);

    // Menampilkan selamat siang
    printf("Selamat Siang, %s\n", nama);

    // Menampilkan selamat malam
    printf("Selamat Malam, %s\n", nama);

    return 0;
}
